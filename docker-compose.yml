# To execute this docker-compose yml file and all of its services, use `docker-compose -f docker-compose.yml up`
# Add the `-d` flag at the end for detached execution
# To stop the execution of all services, hit Ctrl+C, and then `docker-compose -f docker-compose.yml down`
# To execute a specific service, use docker-compose -f docker-compose.yml up <service_name>

services:
  cteleport-automated-tests:
    container_name: cteleport-assignment-container
    environment:
      PYTEST_ARGS:

    shm_size: 4gb
    volumes:
      - ./docker-allure-test-results/:/cteleport-assignment/allure-results
    build:
      context: bash_files
      dockerfile: .

    network_mode: host